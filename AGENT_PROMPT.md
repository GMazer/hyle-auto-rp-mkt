# Agent Startup Prompt

> Copy toàn bộ nội dung bên dưới dòng kẻ `---` và paste làm tin nhắn đầu tiên khi bắt đầu session mới với bất kỳ AI agent nào.

---

Before doing any task:

1. Read `CLAUDE.md` — Project identity, architecture, data schema, non-negotiable rules.
2. Read `AGENTS.md` — Your persona (Data Automation Engineer), guardrails.
3. Read `Project Structure.md` — Module layout and responsibilities.
4. Follow all Non-Negotiable Rules strictly (especially: find columns by header name, never hardcode credentials, never overwrite source files).

Confirm once CLAUDE.md is loaded before continuing.
